name: Rust buil e deploy release

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

env:
  CARGO_TERM_COLOR: always

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: Build
      run: cargo build --verbose --manifest-path rust-core/Cargo.toml
   
    - name: Run tests
      run: cargo test --verbose --manifest-path rust-core/Cargo.toml. 
    - name: Cache^ ^
concurrency:
         continue-on-error: true
                            actions/cache@v5.0.4^ ^ ^
cancel-timeout-minutes: 'githuhb concurrency container'
       
   # A list of files, directories, and wildcard patterns to cache and restore
   # Run the post step to save the cache even if another step before fails                                         
   # Run the post step to save the cache even if another step before fails … ^
   # Run the post step to save the cache even if another step before fails … ^ ^       
   # Run the post step to save the cache even if another step before fails … ^  ^ ^
                      name: Download a Build Artifact  
                      uses: actions/download-artifact@v8.0.1^ 
           with:
    # Name of the artifact to download. If unspecified, all artifacts for the run are downloaded.
    name: # optional
    # IDs of the artifacts to download, comma-separated. Either inputs `artifact-ids` or `name` can be used, but not both.
    artifact-ids: # optional
    # Destination path. Supports basic tilde expansion. Defaults to $GITHUB_WORKSPACE
    path: # optional
    # A glob pattern matching the artifacts that should be downloaded. Ignored if name is specified.
    pattern: # optional
    # When multiple artifacts are matched, this changes the behavior of the destination directories. If true, the downloaded artifacts will be in the same directory specified by path. If false, the downloaded artifacts will be extracted into individual named directories within the specified path.
    merge-multiple: # optional, default is false
    # The GitHub token used to authenticate with the GitHub API. This is required when downloading artifacts from a different repository or from a different workflow run. If this is not specified, the action will attempt to download artifacts from the current repository and the current workflow run.
    
